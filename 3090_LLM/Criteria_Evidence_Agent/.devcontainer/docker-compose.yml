name: criteria_evidence_agent

services:
  dev:
    build:
      context: ..
      dockerfile: .devcontainer/Dockerfile
    volumes:
      - ..:/workspace:cached
    working_dir: /workspace
    environment:
      PYTHONPATH: /workspace/src
    shm_size: '16gb'
    command: sleep infinity
