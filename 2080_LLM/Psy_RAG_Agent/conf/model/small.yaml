# Small model configuration for limited resources
max_features: 3000
hidden_dim: 128
dropout_rate: 0.2
num_labels: 2

# GPU-specific overrides
rtx_3090:
  max_features: 5000
  hidden_dim: 256

standard_gpu:
  max_features: 3000
  hidden_dim: 128

cpu:
  max_features: 2000
  hidden_dim: 64