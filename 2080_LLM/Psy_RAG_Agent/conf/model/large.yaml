# Large model configuration for maximum performance
max_features: 10000
hidden_dim: 512
dropout_rate: 0.4
num_labels: 2

# GPU-specific overrides
rtx_3090:
  max_features: 15000
  hidden_dim: 1024

standard_gpu:
  max_features: 10000
  hidden_dim: 512

cpu:
  max_features: 5000
  hidden_dim: 256