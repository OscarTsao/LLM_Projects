defaults:
  - dataset: baseline
  - model: deberta
  - training: baseline
  - logging: default
  - mlflow: local
  - evaluation: default
  - _self_

seed: 4029

paths:
  output_dir: outputs/trial_0043_rebuild

hydra:
  job:
    chdir: false
  run:
    dir: ${paths.output_dir}/hydra/${now:%Y%m%d_%H%M%S}
  sweep:
    dir: ${paths.output_dir}/hydra/multirun
    subdir: ${hydra.job.num}
